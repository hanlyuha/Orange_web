Add: git add
Commit: git commit -m "comments"
Current git file /home/hanlyuha/web/orange_web
To vu the Log of commits: git log
To reset to the last version: git reset --hard HEAD^
To reset to the version you want: git reset --hard (version number)
To see the Log of every command: git reflog
To sse the git files status: git status
When you got wrong before git add, use: git checkout -- file
When you got wrong after git add before git commit, use: git reset HEAD file
Want to delete a file from version database, use: git rm & git commit -m "remove a file"
git remote add origin git@github.com:hanlyuha/Orange_web.git
git push -u origin master(first time)
git push origin master(after)
To serche for a branch: git branch
To creat a branch: git branch <name>
To chekout a branch: git checkout <name> 
git branch -b <name> to do these things at the same time

Merge <name> branche to the current branche: git merge <name>
To delete a branch: git branch -d <name>
To tag a commit: git tag <name>
To see all the tags: git tag


